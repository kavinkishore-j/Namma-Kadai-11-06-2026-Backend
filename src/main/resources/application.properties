spring.application.name=NammaKadai
server.port=${PORT:8080}

# Actuator
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never

# Database (Supabase PostgreSQL)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?user=postgres.nilctlyecnpwncstvjmm&password=Kavinkishore%406236&sslmode=require}
spring.datasource.username=${DB_USERNAME:postgres.nilctlyecnpwncstvjmm}
spring.datasource.password=${DB_PASSWORD:Kavinkishore@6236}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.open-in-view=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.jmx.enabled=false

logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=INFO

# JWT
jwt.secret=${JWT_SECRET:NammaKadaiSecretKeyForJWTTokenGenerationMustBe256BitsLong!!}
jwt.expiration=86400000

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:duufvqlyd}
cloudinary.api-key=${CLOUDINARY_API_KEY:333634233759571}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:7nXHPI-INkYJQdMXIlhodtxneN0}

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:rkavinkishore@gmail.com}
spring.mail.password=${MAIL_PASSWORD:wtjt gvhc bfxi qtlr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.test-connection=false
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
